name: "Aletheia CodeQL Configuration"

queries:
  - uses: security-and-quality

packs:
  csharp:
    - .github/codeql/extensions

query-filters:
  - exclude:
      id: cs/log-forging

paths-ignore:
  - node_modules
  - _output
  - _tests
  - _artifacts
