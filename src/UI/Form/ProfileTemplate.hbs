<div class="form-group {{#if advanced}}advanced-setting{{/if}}">
    <label class="col-sm-3 control-label">{{label}}</label>

    <div class="col-sm-5">
        <select name="fields.{{order}}.value" validation-name="{{name}}" class="form-control">
            <option value="-1">No Profile Filter</option>
            {{#each selectOptions}}
                <option value="{{value}}">{{name}}</option>
            {{/each}}
        </select>
        <!--<span class="input-group-btn"><button class="btn btn-primary x-profiles-refresh" title="Refresh Profiles"><i class="icon-radarr-refresh" /></button></span>-->
    </div>
    {{> FormHelpPartial}}
</div>
