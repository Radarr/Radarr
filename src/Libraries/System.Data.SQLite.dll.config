<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Override the default map that ships with mono so that 
       we can use a name that works with dotnet core also -->
  <dllmap os="osx" dll="sqlite3" target="libsqlite3.dylib"/>
</configuration>
